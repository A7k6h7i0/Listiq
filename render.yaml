services:
  - type: web
    name: listiq-backend
    env: node
    rootDir: backend
    buildCommand: npm ci --include=dev && npm run build && npm run db:push && npm run db:seed
    startCommand: npm start
    autoDeploy: true
